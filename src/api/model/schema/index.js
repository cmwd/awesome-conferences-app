const mediaItemSchema = require('./media-item-schema');
const socialServiceSchema = require('./social-service-schema');
const eventSchema = require('./event-schema');

module.exports = { mediaItemSchema, socialServiceSchema, eventSchema };
