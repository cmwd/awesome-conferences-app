const conferenceModel = require('../conference-model');

const teardown = () => {
  conferenceModel.remove({});
};

describe('Conference Model', () => {
  it('Should create model from', () => {
  
  });
});
